var f = Number(prompt('Introduce numero de FILA'));
var c = Number(prompt('Introduce numero de COLUMNA'));

  if  (f >= 1 || f <= 6){
  }else{
    alert('Introduza un numero de fila dentro del rango');
  }if (c >= 1 && c <= 6){
  }else{
    alert('Introduza un numero de columna dentro del rango');
}
